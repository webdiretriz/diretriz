{{--componente de clientes--}}
<article class="single">
    <h2 class="d-none">{{$title}}</h2>
    <a href="#">
        <img src="{{url(mix('images/' . $image . '.png'))}}"
             alt="{{$title}}">
    </a>
</article>
